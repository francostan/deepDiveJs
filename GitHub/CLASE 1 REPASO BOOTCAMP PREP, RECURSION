REPASO DEL PREP
RECURSION(mamushka)
funcion que se llama a si misma
siempre es importante que tenga un corte, para que no se llene el call stack
fn recursiva tiene dos aspectos
recordar definir las variables del caso base por parametro
-caso base=condicional
-caso recursivo(llamado a la funcion con el nuevo parametro)

si definimos por parametro, no hace falta que cuando llamemos a la funcion le indiquemos el valor de esos para
metros ya inicializados, le podemos pasar el valor de un solo parametro, pq los otros ya estan inicializados.

PROTOTYPE
siempre que defina una funcion en JS, tendra una propiedad JS
sirve para compartir o heredar metodos a otras funciones contructoras
las instancias(funciones hijas) tienen la propiedad __proto__, que hace referencia a misma lugar de memoria que
la funcion constructora y proptotype
siempre que tengamos una instancia de algo, y llamemos un metodo, primero busca en el __proto__ y si no esta ahi 
busca en el prototype.


